{
  "name": "infra",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "synth": "cdk synth --quiet",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "test": "tsc --noEmit && cdk synth --quiet"
  },
  "devDependencies": {
    "@aws-cdk/aws-lambda-go-alpha": "^2.264.0-alpha.0",
    "@types/node": "^24.0.0",
    "aws-cdk": "^2.1135.1",
    "aws-cdk-lib": "^2.264.0",
    "constructs": "^10.8.1",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
