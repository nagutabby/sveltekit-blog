# HTTPサーバーのリッスンアドレス。既定値は :8080
BACKEND_ADDR=:8080

# ContentServiceが読むMarkdownソースのディレクトリ。既定値は content (backend/content)
CONTENT_DIR=content

# お問い合わせフォームの送信先(Mailtrap)
EMAIL_API_TOKEN=
FROM_ADDRESS=
BCC_ADDRESS=
