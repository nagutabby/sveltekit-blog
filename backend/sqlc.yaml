version: "2"
sql:
  - engine: sqlite
    queries: db/queries
    schema: db/migrations
    gen:
      go:
        package: db
        out: internal/db
        sql_package: database/sql
        emit_json_tags: true
        emit_interface: true
